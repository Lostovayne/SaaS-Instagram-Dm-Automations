import { FC, ReactElement } from "react";

interface DashboardPageProps {
  // Prop types here
}

const DashboardPage: FC<DashboardPageProps> = ({}): ReactElement => {
  // Server action Onboard the user
  // 200 || 201

  return <div>content</div>;
};

export default DashboardPage;
